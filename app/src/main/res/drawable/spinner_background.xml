<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fundo do Spinner -->
    <solid android:color="#FFFFFF" />

    <!-- Borda arredondada -->
    <corners android:radius="8dp" />

    <!-- Borda com cor e espessura -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" />

    <!-- Margem interna -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>